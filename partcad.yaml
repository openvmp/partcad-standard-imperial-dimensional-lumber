parts:
  lumber:
    type: cadquery
    parameters:
      width:
        type: int
        default: 4
      height:
        type: int
        default: 2
      length:
        type: int
        default: 120
  plywood:
    type: cadquery
    parameters:
      width:
        type: int
        default: 48
      length:
        type: int
        default: 48
      thickness:
        type: float
        default: 0.5 # "size" units, close to "inches" but different
